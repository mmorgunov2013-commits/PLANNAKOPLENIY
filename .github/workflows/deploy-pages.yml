name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install
        run: npm ci

      - name: Build static export
        env:
          NEXT_PUBLIC_BASE_PATH: ""
          NEXT_PUBLIC_SITE_URL: https://план-накоплений.рф
          NEXT_PUBLIC_PAYMENT_URL: ${{ vars.NEXT_PUBLIC_PAYMENT_URL }}
          NEXT_PUBLIC_PAYMENT_API_URL: ${{ vars.NEXT_PUBLIC_PAYMENT_API_URL }}
          NEXT_PUBLIC_PAYMENT_INIT_PATH: ${{ vars.NEXT_PUBLIC_PAYMENT_INIT_PATH }}
          NEXT_PUBLIC_PAYMENT_PAGE_URL: ${{ vars.NEXT_PUBLIC_PAYMENT_PAGE_URL }}
        run: |
          export NEXT_PUBLIC_PAYMENT_API_URL="${NEXT_PUBLIC_PAYMENT_API_URL:-https://plan-nakopleniy.ru}"
          export NEXT_PUBLIC_PAYMENT_INIT_PATH="${NEXT_PUBLIC_PAYMENT_INIT_PATH:-/api/tbank_init.php}"
          export NEXT_PUBLIC_PAYMENT_PAGE_URL="${NEXT_PUBLIC_PAYMENT_PAGE_URL:-https://plan-nakopleniy.ru/oplata/}"
          npm run build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - name: Deploy to Pages
        id: deploy
        uses: actions/deploy-pages@v4
